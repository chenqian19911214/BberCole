<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">

    <!-- 填充 -->
    <solid android:color="@color/pink" />

    <!-- 边框 -->
    <padding
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" />

    <stroke
        android:width="6dp"
        android:color="@color/circle_alph" />
    <size
        android:width="33dp"
        android:height="33dp" />
</shape>